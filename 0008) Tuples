# Write a program to count the number of students with the "A" grade in the following tuple.
("C", "D", "A", "A", "B", "b", "A")
CODE:-
    grade = ("C", "D", "A", "A", "B", "b", "A")
    print(grade.count("A"))
OUTPUT:-
    3

# Store this tuple (C, D, A, A, B, b, A) in a list & sort them from "A" to "D".
CODE:-
    grade = ["C", "D", "A", "A", "B", "b", "A"]
    grade.sort()
    print(grade)
OUTPUT:-
    ['A', 'A', 'A', 'B', 'C', 'D', 'b']
