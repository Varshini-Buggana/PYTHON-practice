# Write a program to ask the user to enter names of their 3 favourite movies and store them in a list

CODE:-
    movies=[]
    movies.append(input("Enter 1st movie:"))
    movies.append(input("Enter 2nd movie:"))
    movies.append(input("Enter 3rd movie:"))
    print(movies)
OUTPUT:-
    Enter 1st movie:Majili
    Enter 2nd movie:Arya
    Enter 3rd movie:Oye
    ['Majili', 'Arya', 'Oye']

# Write a program to check if a list contains a palindrome of elements. (Hint: use copy() method)

CODE:-
    list1 = [1, 2, 1]
    copy_list1 = list1.copy()
    copy_list1.reverse()
    if(copy_list1 == list1):
        print("Palindrome")
    else:
        print("Not palindrome")

OUTPUT:-
    Palindrome

CODE:-
    list1 = [1, 2, 3]
    copy_list1 = list.copy()
    copy_list1.reverse()
    if(copy_list1 == list1):
        print("Palindrome")
    else:
        print("Not palindrome")

OUTPUT:-
    Not palindrome

